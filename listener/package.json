{
  "name": "listener",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --hot run src/server.ts"
  },
  "dependencies": {
    "croner": "^8.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
